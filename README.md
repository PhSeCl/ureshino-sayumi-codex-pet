# Ureshino Sayumi Codex Pet

Custom Codex desktop pet for `嬉野纱弓实` (`ureshino-sayumi`).

## Package

- `package/ureshino-sayumi/pet.json`
- `package/ureshino-sayumi/spritesheet.webp`

This package uses the runtime-safe atlas variant, with short animation rows padded to avoid blank-frame hits during state switches.

## QA

- `qa/contact-sheet-runtime.png`
- `qa/contact-sheet-strict.png`
- `qa/previews/`

## Notes

- The package id is `ureshino-sayumi`.
- Display name is `嬉野纱弓实`.
- Outfit uses the school uniform reference.
